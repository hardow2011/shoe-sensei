<div>
    <%= form_with url: filter_models_path, method: :get, class: 'model-filter block' do |f| %>
        <%= render partial: 'models/filter/checkboxes', locals: { title: 'Brand', param_name: 'brand_ids', form: f, filter_list: filter_list[:brands] } %>
        <%= render partial: 'models/filter/checkboxes', locals: { title: 'Activity', param_name: 'activities', form: f, filter_list: filter_list[:activities] } %>
        <%= render partial: 'models/filter/checkboxes', locals: { title: 'Cushioning', param_name: 'cushionings', form: f, filter_list: filter_list[:cushionings] } %>
        <%= render partial: 'models/filter/checkboxes', locals: { title: 'Support', param_name: 'supports', form: f, filter_list: filter_list[:supports] } %>
    <% end %>
    <%= link_to "Clear filters", filter_models_path %>
</div>