<div class="box">
    <div class="columns">
        <div class="column is-narrow is-flex is-align-items-center"><p class="is-size-4"><%= brand.name %></p></div>
        <div class="column">
            <div class="buttons is-right">
                <%= link_to 'Edit', edit_admin_brand_path(brand), class: "button is-link" %>
                <%= button_to 'Delete', [:admin, brand], method: :delete, class: "button is-danger" %>
            </div>
        </div>
    </div>
</div>