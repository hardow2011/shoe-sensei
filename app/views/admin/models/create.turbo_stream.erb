<%= turbo_stream.update "models-filter", partial: 'admin/models/model', collection: @models, locals: { collection: @model.collection } %>
<%= turbo_stream.replace Model.new, partial: 'admin/collections/add_model_btn', locals: { collection: @model.collection } %>
<%= turbo_stream.replace 'flash', partial: 'layouts/flash' %>