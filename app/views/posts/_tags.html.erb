<% if tags.any? %>
    <div class="tags">
        <span><%= t('tags') %>:</span>
        <% tags.each do |tag| %>
            <span class="tag has-background-grey-lighter has-text-black"><%= t("filter.#{tag}") %></span>
        <% end %>
    </div>
<% end %>
