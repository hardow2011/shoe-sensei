<div class="section">
    <div class="container">
        <%= turbo_frame_tag 'filter' do %>
            <%= render partial: 'models/filter', locals: { models: @models, filter_list: @filter_list } %>
        <% end %>
    </div>
</div>